# ============================================================================
# EXAMPLE ENVIRONMENT VARIABLES
# ============================================================================
# 
# SECURITY NOTICE:
# - This file contains PLACEHOLDER VALUES ONLY
# - Real secrets are stored in the platform's secure secret manager
# - NEVER commit real API keys, tokens, or secrets to version control
#
# For Supabase Edge Functions:
# - Set secrets via Supabase Dashboard → Project Settings → Edge Functions
# - Secrets are accessed in Edge Functions via Deno.env.get('SECRET_NAME')
#
# For Frontend (client-side):
# - Only PUBLIC keys (anon/publishable keys) should be in the client bundle
# - The Supabase URL and anon key are PUBLIC and safe to expose
# - NEVER put service role keys or private API keys in frontend code
# ============================================================================

# Supabase Configuration (PUBLIC - safe to expose in client)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your_public_anon_key_here
VITE_SUPABASE_PROJECT_ID=your-project-ref

# ============================================================================
# BACKEND SECRETS (Set in Supabase Edge Function Secrets - NOT in .env)
# ============================================================================
# SUPABASE_SERVICE_ROLE_KEY=***set in secret manager***
# STRIPE_SECRET_KEY=***set in secret manager***
# STRIPE_WEBHOOK_SECRET=***set in secret manager***
# N8N_WEBHOOK_SECRET=***set in secret manager***
# N8N_MESSAGES_WEBHOOK_URL=***set in secret manager***
# N8N_EMAIL_UPDATE_WEBHOOK_URL=***set in secret manager***
# BOOTSTRAP_SECRET=***set in secret manager***
# RUNWARE_API_KEY=***set in secret manager***
# ALLOWED_ORIGINS=***set in secret manager***
